global:
  resolve_timeout: 5m

route:
  receiver: webhook
  group_by: ["alertname", "severity"]
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 5m

receivers:
  - name: webhook
    webhook_configs:
      - url: http://alert-sink:9094/alerts
        send_resolved: true
